# CLAUDE.md — LP / HP 制作AIチーム 運用ルール

このリポジトリは、LP制作・HP制作の受託案件を「AIチーム」で回すための作業場です。
Claude Code はこのファイルのルールを全セッションで前提とします。

---

## 1. このリポジトリの目的

- 受託したLP / HP案件を、ヒアリング → 設計 → 制作 → 検品 → 納品まで一定品質で流す
- 案件ごとの成果物を `clients/<案件スラッグ>/` に隔離する
- 「安く速いだけの制作者」ではなく「機能まで作れる制作者」として納品する

## 2. 絶対に守るルール

### セキュリティ
- **クライアントの実データ（氏名・住所・電話番号・メール）をプロンプトに貼らない。** ダミーデータで開発する
- APIキー・パスワード・DB接続情報をリポジトリにコミットしない。`.env` は必ず `.gitignore` 対象
- 納品前に `git log` と全ファイルを走査し、秘匿情報の混入がないか確認する

### 品質
- 納品物は必ず自分で動作確認する。AI生成コードはエラーハンドリングが抜けがち
- `docs/delivery-checklist.md` を通さないものは納品しない
- 実在の企業ロゴ・有名キャラクター・他社サイトのコピーは使わない。画像はライセンスを確認する

### 表記
- 制作物内に「AIが作りました」等の記載は不要だが、**クライアントに聞かれたら正直に答える**
- 成果物の著作権・改変可否は見積り時に合意しておく

## 3. ディレクトリ構造

```
.claude/agents/lp/    LP制作チーム（5役）
.claude/agents/hp/    HP制作チーム（5役）
.claude/skills/       職人技のスキル層（6種）。各エージェントにプリロードされる
.claude/commands/     スラッシュコマンド（/lp-build, /hp-build, /proposal）
docs/                 ヒアリングシート（LP用/HP用）・検品リスト・料金表
clients/<案件名>/     案件ごとの作業ディレクトリ
clients/_template/    新規案件の雛形
```

## 4. 案件の進め方（標準フロー）

1. `clients/_template/` を `clients/<案件スラッグ>/` にコピー
2. **案件種別に応じたヒアリングシートでクライアントに聞く**
   - LP → `docs/hearing-sheet-lp.md`
   - HP → `docs/hearing-sheet-hp.md`
   - **この2つは聞くことが違う。** 流用すると事故要因を取りこぼす
3. `brief.md` に募集要項・ヒアリング内容を貼る
4. `/lp-build` または `/hp-build` を実行
5. ディレクターが各エージェントに委譲し、`clients/<案件スラッグ>/` 配下に成果物を出力
6. `docs/delivery-checklist.md` で検品してから納品

## 5. スキル層

`.claude/skills/` に、一流の職人技を6つ切り出してある。**技はスキル、手順はエージェント**という層分け。

| スキル | 内容 |
|---|---|
| `copy-craft` | 抽象語の潰し方、削り方、見出しの作り方、リズム |
| `art-direction` | 配色・余白・階層・引き算・「AIが作った感」の回避 |
| `japanese-typography` | 行長・行間・約物・禁則・和欧混植・Webフォントの軽量化 |
| `ux-writing` | ボタン文言、ラベル、エラー、空状態のマイクロコピー |
| `cro` | ファーストビュー、フォーム離脱、不安の除去、CVR |
| `photo-direction` | 素材の見極め、撮影指示書、トリミング、権利、AI画像の扱い |

各エージェントの `skills:` フロントマターでプリロードされるため、担当は自動で該当の技を持った状態で起動する。
**同じ内容をエージェント側に重複して書かない。** 技を直すときはスキルだけを直せば、全エージェントに伝播する。

## 6. 技術方針

- **デフォルトは静的HTML/CSS/JS 1〜数ファイル構成。** フレームワークは要件がある時だけ
- フォーム・予約・通知などの機能要件がある場合のみバックエンドを足す（差別化ポイント）
- ホスティングはVercel / Netlify / Cloudflare Pages を想定
- レスポンシブ（375px〜）、キーボードフォーカス可視、`prefers-reduced-motion` 対応は品質の床。宣言せず当然やる

## 7. 見積りの原則

- **¥19,800 の価格競争には参加しない。** 機能込みで ¥25,000〜 を下限とする
- プラットフォーム手数料（約20%）を必ず単価に織り込む
- 修正回数の上限を見積り段階で明記する（標準2回まで）
- 保守・追加機能の提案を納品時に必ず1回行う（ストック収益の入口）
